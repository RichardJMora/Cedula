'use strict';

describe('cedulaApi.creditos module', function() {

  beforeEach(module('cedulaApi.creditos'));

  describe('creditos controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var creditosCtrl = $controller('creditosCtrl');
      expect(creditosCtrl).toBeDefined();
    }));

  });
});